# 图片 OCR 整理到 Notion

中文 | [English](SKILL.md)

从本地图片或小红书公开分享链接提取文字，在 macOS 本机运行 OCR，忠实清理识别结果，并可在用户授权后保存到 Notion。

## 判断处理路线

- 本地图片或目录：直接运行本地 OCR。
- 小红书公开分享链接：先下载帖子图片，再运行 OCR。
- 已有 OCR 文本：跳过识别，从文本清理开始。
- 用户指定 Notion 目标：准备页面并展示简短预览，获得写入授权后再操作。

不得从真实浏览器配置中读取 Cookie、Token 或浏览器数据，不得绕过登录、反爬或访问控制。公开分享页面不再暴露帖子时应停止。

## 1. 获取图片

本地输入需要逐一解析并验证路径。处理小红书公开链接时，在隔离环境中安装可选依赖后运行：

```bash
python3 scripts/download_xhs_images.py "<公开分享链接>" --output "<输出目录>"
```

脚本生成包含编号图片和 `metadata.json` 的目录。失败或涉及访问、版权问题时，阅读[小红书公开链接说明](references/xiaohongshu.zh-CN.md)。

## 2. 本地 OCR

```bash
python3 scripts/ocr_images.py "<图片目录>" --output "<OCR 输出文件>"
```

脚本调用本机 Apple Vision，按文件名顺序处理图片，不会将图片上传到 OCR 服务。

## 3. 清理文字

- 合并明显由 OCR 造成的断行；
- 只有图片或上下文足以确认时才纠错；
- 保留标题、列表、引用、人名、数字和 URL；
- 无法确认的文字要标注，不能猜测；
- 删除导航界面和孤立识别噪声。

原始 OCR 和清理结果应同时保留，方便后续复核。

## 4. 保存到 Notion

使用目标环境中已有的 Notion MCP、CLI 或 API。不得在 Skill 中写入凭据或数据库 ID。写入前展示标题、目标位置、来源 URL 和内容预览；获得授权后创建或追加页面，保留来源与采集日期，并回读标题和代表性内容。

完成时确认：原始与清理文本均存在；没有读取浏览器配置或私密 Token；清理内容没有凭空补充；所有 Notion 写入都已授权并回读。
